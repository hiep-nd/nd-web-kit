# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

workspace 'NDWebKit'

target 'Samples' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Samples
  pod 'NDWebKit', :path => '.'

  pod 'NDLog/ObjC', :path => '../NDLog'
  pod 'NDModificationOperators/ObjC', :path => '../NDModificationOperators'
  pod 'NDUtils/Foundation', :path => '../NDUtils'
  pod 'NDUtils/libextobjc', :path => '../NDUtils'
  pod 'NDUtils/objc', :path => '../NDUtils'

  target 'SamplesTests' do
    # Pods for testing
  end

end
